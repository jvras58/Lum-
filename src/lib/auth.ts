// Placeholder for v1 — authentication is out of scope per spec assumption.
// Replace with real session validation (e.g., NextAuth, Clerk) in a future version.
export function assertAuthenticated(): void {
  // no-op for v1
}
